# ── LaRa Core Dependencies ────────────────────────────────
pyyaml>=6.0
numpy<2
requests>=2.31.0

# ── Core HPC & Audio ─────────────────────────────────────────
sounddevice>=0.4.6
webrtcvad>=2.0.10
faster-whisper==1.0.2            # CUDA-native STT (replaces pywhispercpp)
CTranslate2==4.3.1               # Backend for faster-whisper (GPU support)
pyaudio>=0.2.14

# ── LLM (Ollama client is plain HTTP, no extra dep needed)

# ── TTS ───────────────────────────────────────────────────
kokoro>=0.3.0

# ── ML / Torch ────────────────────────────────────────────
# For A100 / CUDA 12.x, install torch separately:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
torch

# ── Vision Perception ─────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
opencv-python-headless
mediapipe>=0.10.11
ultralytics>=8.2.0               # YOLOv8 for face detection
onnxruntime-gpu>=1.17.0 ; sys_platform == 'linux' # GPU inference for A100
onnxruntime>=1.17.0 ; sys_platform == 'darwin'    # CPU inference for macOS testing
insightface>=0.7.3 # Lightweight face embedding

# ── Science / Math ────────────────────────────────────────
scipy>=1.13.0
numpy
transformers>=4.40.1

# ── Memory / RAG ──────────────────────────────────────────
chromadb>=0.4.22

# ── Monitoring ────────────────────────────────────────────
psutil>=5.9.8

# ── Testing ───────────────────────────────────────────────
pytest>=8.0.0
httpx>=0.27.0                   # Required by TestClient in FastAPI tests
insightface>=0.7.3 mysql-connector-python>=8.0.0
mysql-connector-python>=8.0.0
